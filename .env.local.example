# Environment configuration for CMS Nova (English)
# Copy this file to .env and adjust values for your environment.

# Database
# Example for PostgreSQL running locally
DATABASE_URL="postgresql://user:password@localhost:5432/cms_nova"

# Authentication (Better Auth)
# In production, use a secure random string (at least 32+ chars)
# How to generate a strong secret:
# - Node.js (recommended):
#     node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# - OpenSSL:
#     openssl rand -hex 48
BETTER_AUTH_SECRET="change-me-in-production"
# Base URL of your app (used by Better Auth)
# ⚠️ CRITICAL: In production, this MUST match your domain exactly
# Examples: "https://myapp.com", "https://myapp.vercel.app", "https://myapp.coolify.com"
# 📌 SEO TIP: Use domain WITHOUT www for better SEO (e.g., "https://yourdomain.com")
BETTER_AUTH_URL="http://localhost:3000"

# Public App URL (used on the client side and for CORS)
# ⚠️ CRITICAL: In production, this MUST be your actual domain
# This is used by the Better Auth CLIENT to make API calls
# Examples: "https://myapp.com", "https://myapp.vercel.app"
# 📌 SEO TIP: Use domain WITHOUT www for better SEO (e.g., "https://yourdomain.com")
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Include both www and non-www variants in trustedOrigins
# Set to 'true' only if you need both domains to work during migration/testing
# INCLUDE_WWW_VARIANT=false

# Optional: Coolify specific URLs (if provided by Coolify)
# COOLIFY_URL=""
# APP_URL=""

# Encryption (required)
# Must be a 64-character HEX string (32 bytes) for AES-256-GCM.
# How to generate a 64-hex key (choose one):
# - Node.js:
#     node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - OpenSSL:
#     openssl rand -hex 32
# Paste the generated value below:
ENCRYPTION_KEY="<paste-64-hex-key-here>"

# AWS S3 is configured from the Plugins panel (/admin/dashboard/plugins)
# No need to set AWS envs here unless you prefer env-based config.
# If needed, uncomment and fill:
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET=""
